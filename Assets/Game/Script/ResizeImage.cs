
using UnityEngine;
using UnityEngine.UIElements;

public class ResizeImage : MonoBehaviour
{
    public void ResizeImageByWidth(Image icon, float target = 50)
    {

        // RectTransform rectTransform = icon.rectTransform;
        // float aspectRatio = icon.sprite.rect.width / icon.sprite.rect.height;
        // float targetHeight = target / aspectRatio;


        //rectTransform.sizeDelta = new Vector2(target, targetHeight);
    }
}
